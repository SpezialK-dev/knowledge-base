```
dos2unix [options] [FILE ...] [-n INFILE OUTFILE ...]  
unix2dos [options] [FILE ...] [-n INFILE OUTFILE ...]
```

as taken form https://manpages.org/dos2unix

its only purpose is to convert DOS files to unix and whise versa can be helpfull when using weird git patches. 